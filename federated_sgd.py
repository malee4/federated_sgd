import numpy as np

# clear up data

def aggregateMatrix(LG_set = np.array([])):
    # if nothing is passed in
    if LG_set.size == 0:
        return None


    return

def updateV(LG, V, alpha=0.1, beta=0.01):
    if LG.size == 0:
        return 
    elif LG[0].size == 0:
        return
    for i in LG.size:
        for j in LG[0].size:

    return



